pipeline {
    agent any
    stages {

      stage('Build') {
        steps {
          git branch: 'main', url: 'https://github.com/AntoninaRozhok/Py_script.git'
        }
      }
      stage('Test') {
        steps {
          echo 'Testing using Robot Framework'
          sh 'robot test.robot'
        }
      }
           stage('Monitor') {
     steps {
       echo 'Well done!'
     }
   }
    }    
  }
